# Changelog

本專案的所有重要變更都記錄於此。格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/),版本遵循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

## [0.27.0] - 2026-07-29

白板:無限畫布,存成任何工具都讀得懂的 JSON Canvas。

### 新增

- **白板(`.canvas`)**:側欄右鍵「新增白板」即建立。無限畫布上可放四種節點——文字(輕量 Markdown)、筆記(顯示標題與內容預覽,點標題直接開)、連結、群組——並在節點之間拉線。
- **互動**:拖曳移動(對齊格線)、四角調整大小、框選與 Shift 複選、從節點邊緣的接點拖出連線、六色色票與清除、`Delete` 刪除、`Cmd/Ctrl+Z` 復原、滾輪平移、`Cmd/Ctrl`+滾輪以游標為錨縮放、`⤢` 全部顯示。從側欄把筆記拖到畫布上即成為一個筆記節點。
- **格式為 [JSON Canvas 1.0](https://jsoncanvas.org)**:與 Obsidian 等工具互通,不是自訂格式。
- **白板是知識庫的一等公民**:白板上的筆記節點算一條連結——被引用的筆記在反向連結面板看得到「白板上的節點」,關聯圖也畫得出白板(方形節點,與筆記的圓形區分)。筆記改名時,白板裡指向它的路徑跟著改寫。白板上的文字進得了全文搜尋。
- 白板與筆記共用同一條管線:端對端加密同步、版本回溯(時光機)、空間歸屬、`.stele` 的 CRDT 持久化全部原封適用,不需要為白板另外設定任何東西。

### 設計

- **看不懂的欄位原封帶回**。JSON Canvas 是開放格式,同一份檔案可能先被別的工具寫過。解析時不認識的欄位(乃至型別不合預期的已知欄位)一律保留,存檔時照樣寫出去——否則使用者只是在 Stele 裡拖了一下節點,回到別的 app 就發現資料沒了。
- **壞資料丟到節點為止**。單一節點缺座標、`type` 沒見過,丟那一個節點;只有「整份根本不是 JSON 物件」才算解析失敗。一張白板不該因為一個爛節點而打不開。
- **沿用文字 CRDT 而非另造一套白板 CRDT**。整份 JSON 存在既有的 `Y.Text`,寫回時只送差異。代價寫在明處:併發合併發生在 JSON 文字層,兩人同時拖不同節點會正常收斂,但極端情況下有機會讀到語法半成品——因此解析失敗時畫面停在最後一個可用版本並**停止寫回**,絕不拿壞資料覆蓋別人的檔案。逐節點欄位級合併(真正的多人同拖)不在這一版。
- **只在手勢結束時寫檔**。拖曳過程僅在本地投影;滑鼠每動一格就寫回,會讓同步管線與版本歷史塞滿沒有意義的中間狀態。
- **節點是 DOM 而非 2D 繪製**:文字要能選取、能編輯、能被輔助技術讀到。群組的中間刻意「透明」,否則框住的節點就再也點不到。
- **節點文字採輕量 Markdown 呈現**,只認白板上實際會用到的那幾種標記,且產出 React 元素而非 HTML 字串——節點內容來自協作者,`innerHTML` 等於把注入面開給同步管線上的每一個人。
- **連結節點只放行 http/https**:把任意 URL 交給系統 shell,等於把本機程式的啟動權交給任何能寫進這個 vault 的人。

### 品質

- 白板 74 項新測試:格式讀寫 24 項(往返保真、未知欄位、壞資料逐項丟棄、懸空邊、序列化順序)、幾何 19 項(縮放以游標為錨、自動選側、群組命中規則、調整大小不翻面)、編輯操作 20 項(刪節點連帶刪邊、群組帶著成員走、清顏色是拿掉欄位而非寫空字串、編輯後往返一致)、vault 管線 11 項(白板進清單與索引、改名連動、壞白板不擋索引重建、查詢視圖不把白板當頁面)。全庫 700 項測試全綠。
- smoke 增至 20 項:真機驗白板建立 → 節點編輯 → 放上筆記 → **拖曳** → **拉線**,最後檢查磁碟上的 `.canvas` 是合規 JSON 且該筆記的反向連結認得這張白板。
- 走查抓到一個真缺陷:點畫布空白處會直接卸載編輯框,而 React 卸載**不保證**觸發 blur——「打完字點旁邊」這個最自然的動作會讓內容無聲消失。改為卸載即提交。

## [0.26.0] - 2026-07-29

查詢視圖:把筆記當成可查詢的資料庫。

### 新增

- **`stele-query` 程式碼區塊**:在所見即所得模式下渲染成查詢結果,點結果列直接開啟該篇筆記。語法對標 Dataview 的實用子集:

  ```
  LIST | TABLE 欄位 [AS 別名], …
  [FROM #標籤 | "資料夾" | -否定 (AND|OR …)]
  [WHERE 條件 (AND|OR …)]
  [SORT 欄位 [ASC|DESC]]
  [LIMIT n]
  ```

  例:`TABLE status, priority AS 優先度 FROM #工作 WHERE status = "進行中" SORT priority DESC`
- **可查詢的資料**:frontmatter 的所有欄位、內文與 frontmatter 的 `#標籤`,以及隱含欄位 `file.name` / `file.path` / `file.folder` / `file.tags` / `file.mtime`。
- 比較運算子 `=` `!=` `>` `<` `>=` `<=` `contains`;`FROM #專案` 也會命中子標籤 `#專案/Stele`,`FROM "資料夾"` 含子資料夾。

### 設計

- **只做子集,而且說清楚**:Dataview 還有 TASK、CALENDAR、函式、inline field、運算式。與其半套地假裝相容,不如語法少而準——查詢給出**看似合理但錯誤**的結果,比查不到更糟。所以語言標記用 `stele-query` 而不是 `dataview`,不讓人誤以為可以照搬。
- **查詢只在所見即所得模式渲染,源碼模式維持原始文字**:順著既有的雙模式分工,要改查詢就切到源碼模式(Cmd/Ctrl+E)。比在渲染結果裡塞一個編輯狀態單純,也沒有「點哪裡才進得去」的猜謎。
- **序列化完全不受影響**:這是 ProseMirror 的 node view,寫回磁碟的仍是原本的 code fence,位元組不變。
- frontmatter 走真正的 YAML 解析器(js-yaml,MIT)。使用者的 frontmatter 會有清單、巢狀、引號與日期,手寫的子集解析器必然在某個奇怪的地方給出錯誤答案。
- 缺席的欄位一律讓比較不成立:沒有那個欄位的筆記不會悄悄混進結果;排序時缺值一律沉底。

### 品質

- 查詢語言 24 項測試(解析錯誤訊息、FROM/WHERE/SORT/LIMIT 各分支、缺欄位行為、空結果),資料層 13 項(YAML 壞掉不炸、標籤不吃 code fence 與標題、純數字不算標籤)。全庫 626 項測試全綠。
- 走查以真的 vault 驗完整條路:TABLE 別名與排序、LIST、寫錯的查詢顯示明確原因、點結果開啟筆記,並確認切到源碼模式看得到查詢原文(序列化沒被破壞)。

## [0.25.1] - 2026-07-29

大 vault 規模驗證與修復:反向連結從 183 ms 降到 0.9 ms。

### 修正

- **反向連結與關聯圖在大 vault 會慢到有感**:兩者都要對全庫**每一個** wikilink 各解析一次,而解析是逐次線性搜尋整份檔案清單——總成本是 O(檔案數 × 連結數)。1000 篇的 vault 實測 183 ms,而反向連結掛在**每次開啟筆記**的路徑上。改用預先建好索引的解析器後降到 0.9 ms(200 倍),關聯圖 184 ms → 3.3 ms。
- 更重要的是複雜度從二次降為線性:5000 篇實測 4.6 ms(規模 5 倍、時間也 5 倍)。照原本的寫法,同樣規模會是 4.6 秒。

### 品質

- 以 1000 篇 / 5000 篇、含三萬個歷史版本檔的 vault 實測全部主要路徑。其餘都健康:全庫清單 15 ms、搜尋 9 ms、時光機版本清單 0.1 ms、5000 篇啟動 2.5 秒、打字無延遲。
- 一併確認了**時光機不是效能問題**:原本擔心版本記錄掛在每 120 ms 的鏡像落盤上會拖慢編輯,實測掃描一篇筆記的 50 個版本檔只要 0.13 ms(每秒約 2 ms)。差點去改一個不存在的問題,先量是對的。
- 新解析器與逐次解析的語意一致性寫成測試(同一組目標兩邊結果必須相同),避免兩條路徑對同一個連結給出不同答案。全庫 589 項測試全綠。

## [0.25.0] - 2026-07-28

筆記版本回溯(時光機):翻回上週那一版,看清楚差在哪,再決定要不要還原。

### 新增

- **自動留版本**:編輯過程中會定期把筆記存成一份快照,關檔與切換筆記時也會補一份——短暫的編輯 session 不會完全沒有歷史。
- **版本面板**:筆記右上的 🕘 開啟。左側選版本,右側逐字標出它與目前內容的差異,確認後才還原。
- **還原按錯救得回來**:還原前會先把**現況**存成一個版本,所以還原本身也是可還原的。
- **不佔滿磁碟**:近期版本密集保留,越久越稀疏(一小時內全留、一天內每小時、一個月內每天、更久每週),另有每篇筆記的數量上限。

### 設計

- **版本存成純 Markdown,不是 CRDT 二進位**:落在 `.stele/history/<筆記>/<時間戳>.md`,用檔案總管就能翻、用任何編輯器都讀得懂。即使哪天不用 Stele 了,歷史仍是一疊純文字——這是「刻下來的知識不依賴任何人」的一部分。
- **為何不用 Yjs 快照**:那需要永久關閉垃圾回收,既有筆記早已回收的內容救不回來,而且會讓長期編輯的筆記狀態無限膨脹。純文字快照對既有 vault 立刻生效,體積也可控。
- 純本地功能:不需要同步、不需要團隊,離線可用。還原走 CRDT 的最小差異,所以協作者會即時看到,游標也不會全部跳掉。

### 修正

- **`.stele` 底下的檔案不再被當成筆記**:版本快照用 `.md` 副檔名,而 vault 掃描原本會遞迴收走所有 `.md`——歷史版本因此出現在側欄與搜尋裡,還會被配上 doc id 上傳到同步伺服器。現在掃描與監看一律略過隱藏目錄(`.stele`、`.git`、`.obsidian` 等)。

### 品質

- 儲存層 14 項測試涵蓋稀釋分層、節流、內容未變不記、路徑穿越防護;另加「`.stele` 不得出現在筆記清單」的回歸測試。全庫 586 項測試全綠。
- 走查跑完「編輯 → 關檔 → 重開再改 → 開面板 → 看差異 → 還原」,並確認還原後編輯器畫面同步更新。上面那條 `.stele` 汙染就是走查截圖才看見的——測試斷言全過,但畫面上側欄多了一列歷史檔路徑。

## [0.24.0] - 2026-07-28

一次入職:組織一道指令為旗下多個團隊批次產出邀請碼,並看得見誰還卡在哪個團隊待核准。Slice 3 的跨團隊管理台到此完整。

### 新增

- **一次入職(批次產碼)**:組織用一道指令為旗下多個團隊各產一張邀請碼(`pnpm org invite`),不必逐一去拜託每位團隊擁有者。另有 `pnpm org pending` 看跨團隊的待核准佇列——誰還卡在哪個團隊一目了然。

### 修正

- **協作頭像列跑到右側面板上**:在場頭像用絕對定位,卻沒有任何相對定位的祖先,於是錨到整個視窗右上角、壓在反向連結面板上方。改為與模式切換鈕同住一個容器,如註解所述真正固定在**編輯區**右上。
- **窄視窗下側欄橫向溢出**:標題列的按鈕已累積到七八個,放不下時改為換行。同時修掉標題的 `min-width`——flex 項目預設不肯縮到內容寬以下,原本設好的截斷省略號其實從未生效。

### 安全

- **產碼不等於加入,而且說清楚**:組織產的碼只讓對方進入各團隊的**待核准**佇列;真正的核准是把團隊金鑰包給他,那需要 root——治理模式下組織沒有,做不到也不假裝做得到。CLI 每次產碼都會把這件事印出來,不讓「邀請」被誤讀成「已加入」。碼一律不能帶擁有者角色。
- **升級三個有已知弱點的建置期依賴**:`brace-expansion`(DoS)、`fast-uri`(host confusion)、`tar`(遞迴 DoS)。三者都只出現在建置工具鏈裡(eslint 與 electron-builder 的傳遞依賴),**沒有進入桌面 app 或伺服器的產出**,因此對使用者無實際暴露;仍然升級,因為建置工具跑在有發布權限的機器上。`brace-expansion` 的 1.x/2.x 分支已無維護、沒有修復版,只能整樹釘到 5.0.8。
- 驗證方式:除了全庫測試與 smoke,另外**實際跑一次 electron-builder 打包**——受影響的三個套件都在它底下,只跑測試看不出打包是否壞掉。

### 品質

- 端到端測試涵蓋:批次產碼每團隊一張且附當代擁有者公鑰、**用碼只到待核准而拿不到金鑰**、碼的一次性、指定他組織團隊被拒且與「不存在」的訊息一字不差、碼發不出擁有者角色、一般成員連線送批次產碼被拒。全庫 571 項測試全綠。
- 走查驗證了 CLI 與 app 之間的接縫:用**真的 CLI 產出的碼**在真的 app 裡加入,確認回報「等待擁有者核准」而非就緒。單元測試是直接呼叫 bootstrap,走不到這條路。
- 走查另抓到:新增事件種類後忘了補人話標籤,管理員在 CLI 看到原始代號 `enroll-batch-created`。改為種類與標籤同住一個檔案,並加完備性測試——漏了就紅。

## [0.23.0] - 2026-07-28

管理事件彙整:組織能在一處看見旗下所有團隊是**怎麼被管理的**。

### 新增

- **管理事件記錄**:成員加入、核准、被移除、改角色、金鑰輪換、認領與撤換擁有者、綁定組織、組織政策、一次全撤,都會留下一筆帶時間、發起人與對象的紀錄。
- **`pnpm org events`**:組織用自己的根金鑰連線,拉出全組織或指定團隊的事件,新到舊列出。

### 安全

- **兩條邊界寫在輸出裡,不只寫在文件裡**:每次列出事件都會附帶說明——①**只涵蓋管理動作,看不到任何內容操作**(誰讀寫了哪篇筆記都在密文裡,組織解不開);②**這是伺服器自己的紀錄,不是密碼學證據**,能竄改伺服器的人也能竄改這份日誌。要做成證據需要發起人簽章與雜湊串鏈,那是另一件事,不在這裡半套實作。
- **事件歸屬是發生當下的組織快照**,不是查詢時才回推目前綁定。否則團隊換組織後,新組織會讀到它在前一個組織旗下時期的管理史,舊組織也會看到自己失去該團隊之後的事件。未綁組織時期的事件不歸任何組織,誰都拉不到。
- **只限組織管理連線**:一般團隊成員的連線拉不到——這份清單橫跨組織全部團隊,不是單一團隊成員該看見的範圍。跨組織指名他人的團隊一律撈不到東西。
- 每個團隊的事件量有保留上限,不無界成長。

### 修正

- **核准只記首次**:原本每次重簽成員憑證都記一筆,於是一次金鑰輪換就替每位成員各記一次「核准」,真正的管理動作被例行換鑰的流水帳淹沒。改為只在成員**首次**取得金鑰信封時記錄(輪換的重包與擁有者給自己的信封都不算)。

### 品質

- 端到端測試涵蓋:事件時序與歸屬、改角色/輪換/一次全撤的落點、跨組織隔離(連指名他組織的團隊也撈不到)、一般成員連線被拒、**寫入筆記不產生任何管理事件**、輪換不重複記核准。全庫 558 項測試全綠。
- 上面那條「核准雜訊」是真機走查抓到的:測試只驗事件有落地,要把 CLI 實際輸出印出來看,才發現這份清單對管理員而言噪音大於訊號。已固化為回歸測試。

## [0.22.0] - 2026-07-28

游標名簽章:協作者游標旁的名字從「自己說了算」變成可驗證的身分。

### 新增

- **在場宣告帶簽章**:團隊成員在某篇筆記上宣告在場時,對「我是這位成員、用這個名字、這個顏色」簽章。收件端查擁有者背書的成員目錄取得可信公鑰後驗章,驗過才顯示為已驗證身分。
- **已驗證徽章**:驗過的協作者,在場頭像帶 ✓ 角標、遠端游標標籤帶「✓ 名字」——與留言作者的已驗證徽章同一套語彙。
- **名字優先組織名冊**:驗過身分的成員,游標與在場列顯示組織背書的名字(而非成員自選名);tooltip 明說這個名字憑什麼可信——組織背書、或僅身分可驗而名字自選、或完全未驗證。

### 安全

- **補上先前明載的誠實範圍**:0.16.0 曾記錄「協作游標的名字仍以自我宣告的形式傳輸,因此不加已驗證徽章」。這個缺口現已關閉——名字是協作介面上最容易被冒用的東西,先前卻完全沒有驗證。
- **簽章綁 clientId,擋複製冒名**:在場狀態以 Yjs client id 為槽位鍵,不綁的話任何成員都能把他人的簽章原封複製到自己的槽位、顯示成那個人。同時綁 docId(防跨筆記挪用)與金鑰紀元(輪換即讓被撤者的舊宣告失效)。
- **未驗證的 memberId 一律不外流**:只是「自稱」某個 memberId 的在場宣告,其 memberId 會被抹掉才交給介面。否則冒稱他人身分就能借到組織名冊對那個名字的背書,做出比自報名字更可信的假身分。
- **簽了卻驗不過即整筆丟棄**(那是主動偽造),未簽的則沿用過渡容忍、標為未驗證——與逐 update 寫入驗證同一套策略;強制簽章模式下連未簽的在場也不顯示。
- **誠實邊界**:簽章綁的是**身分,不是游標位置**。合法成員仍可謊報自己的游標落在哪一段——那不構成冒名,是刻意接受的殘留面。在場也只證明「這個名字屬於這位成員」,不證明「此人此刻在線」。SECURITY.md §2.10 完整記載。

### 品質

- 端到端測試涵蓋:真成員的名字帶驗證標記、合法成員複製他人簽章到自己槽位被整筆丟棄、未簽的舊版用戶端顯示為未驗證且 memberId 不外流、強制簽章模式下未簽在場不顯示;簽章層另有跨 doc/紀元/clientId/名字/顏色的竄改測試。全庫 542 項測試全綠。
- 雙實例真機走查:組織只替其中一方命名,於是同一輪就同時驗到兩種狀態——一方看到「組織名 + 徽章」,另一方看到「自選名 + 徽章」,並確認游標標籤實際渲染出「✓ 陳小美」。

## [0.21.1] - 2026-07-28

雙實例走查修復:用兩個獨立身分的實例走完組織全流程(建團隊 → 邀請 → 核准 → 綁組織 → 命名 → 撤換擁有者 → 接管 → 一次全撤),修掉兩個成員側才看得到的問題。

### 修正

- **被移出團隊後,畫面上完全沒有提示**:用戶端其實收到了伺服器的「已被移出」通知,但緊接著的斷線狀態把提示蓋掉,使用者只看到「離線」,不知道自己已失去存取(走查等了 45 秒都沒出現)。現在「已被移出」是終局狀態:後續的連線狀態一律不覆蓋它,重新查詢與重開 app 也都一致顯示。
- **在線的擁有者不知道組織要求輪換**:一次全撤後,原本只有重開 app 才看得到催促。改為伺服器即時推送給留任成員,擁有者當下就會在團隊面板看到提示。
- **接管重簽後角色顯示不同步**:接管者的面板會停在舊角色(顯示為編輯者)直到下次重連,雖然管理功能已生效。現在接管當下即更新。

### 已知行為

- 組織在名冊上改名後,成員要到**下次連線**才會看到新名字(不是即時推送)。改名不影響任何權限,故未加推送。

### 品質

- 走查方法固化:兩個實例以獨立的使用者資料目錄啟動,直接驅動 app 的 IPC(等同點按鈕),並以 DOM 檢查與截圖驗證畫面——這一輪抓到的兩個問題,全庫 526 項測試都測不出來。全庫 528 項測試全綠。

## [0.21.0] - 2026-07-27

離職一次全撤:組織一個動作,把某人從旗下**所有**團隊移除並當場中斷連線;以及組織級的安全政策。

### 新增

- **一次全撤**:組織可將某位成員從自己的所有團隊一次移除,活躍連線當場切斷,重連即被拒。回報明列處理了哪些團隊。
- **不會斬首團隊**:若對方正是某個團隊的擁有者,該團隊會被**略過並明確回報**——移除擁有者會讓團隊無人能簽發憑證與金鑰信封。組織應先指派新擁有者,再撤。
- **待輪換催促**:全撤後,各團隊的擁有者會在團隊面板看到提示,說明「對方手上的舊金鑰仍解得開既有內容,請輪換」。輪換完成即自動消除。
- **組織級強制簽章**:組織可對旗下所有團隊要求簽章寫入,與各團隊自己的設定**取較嚴者**。
- CLI 新增 `pnpm org revoke`(一次全撤)與 `pnpm org policy`(組織級強制簽章)。

### 安全

- **誠實的能力邊界**:移除是伺服器層、即時生效;但被撤者手上的舊金鑰要**輪換**才真的失效,而輪換需要團隊金鑰——只有團隊擁有者做得到。因此組織的動作是「即時切斷存取 + 標記催促」,而不是假裝一鍵完成。介面與 CLI 都明說這一點,不讓人誤以為撤除等於資料已對其關閉。
- **組織只能加嚴、不能放寬**:組織政策與團隊政策取較嚴者。若允許放寬,一張真簽但寬鬆的組織政策就能鬆綁某個團隊已開啟的強制簽章。
- 組織政策帶單調序號,伺服器強制遞增,擋回放舊政策;非組織鏈簽的政策一律驗不過。
- 全撤與政策下發僅限組織管理連線;成員連線嘗試這些動作一律拒絕。

### 品質

- 端到端測試涵蓋:跨團隊移除與踢線、擁有者團隊被略過、待輪換旗標的設定與輪換後自動清除、被撤者重連被拒、組織政策取較嚴(團隊已開時組織關閉不會放寬)、政策序號回退被拒。全庫 526 項測試全綠。

## [0.20.1] - 2026-07-27

真機走查修復:把 app 實際開起來、跑完「建團隊 → 組織簽發 → 綁定」全程並檢查畫面,修掉三個只有在真的渲染出來才看得見的問題。

### 修正

- **文案的變數沒有被代入**:團隊面板的組織區塊直接把 `{key}`、`{orgId}`、`{serial}` 原樣顯示給使用者——插值語法寫成單大括號,而 i18next 用的是雙大括號。編譯與型別檢查都攔不住這種錯,只有渲染出來才看得見。
- **身分公鑰無法好好複製**:公鑰原本黏在說明文字裡、會在中間斷行,而它正是要交給組織去簽發憑證的東西。改為比照邀請碼的唯讀輸入框加複製按鈕(按鈕文字也從誤用的「複製邀請碼」改為「複製」)。
- **「綁定組織」按鈕被擠成兩行**:輸入框與按鈕的伸縮沒設定好。按鈕一律不折行、輸入框可收縮。

### 品質

- 插值格式固化為回歸測試:任何語系的任何文案再用單大括號佔位就會測試失敗,並檢查同一 key 在各語系的插值變數一致(漏帶變數會顯示空白)。全庫 516 項測試全綠。

## [0.20.0] - 2026-07-27

組織名冊與跨團隊總覽:協作與留言上的名字,從人人可自稱的字串升級為組織背書、可驗證的身分。

### 新增

- **組織名冊**:組織可為每位成員背書顯示名與部門,成員端逐筆驗章後採用。顯示名的優先序是**組織名冊(可驗)> 成員自選名 > 訪客-xxxx**;驗不過的條目一律濾掉,寧可回退成自選名,也不顯示任何未經組織背書的名字。名冊只是「組織說這個人叫什麼」——不含金鑰、不影響任何授權。
- **留言作者顯示組織名**:作者身分自 0.17 起已是密碼學可驗,現在名字也一併可信。
- **加入時就套用組織名**:綁組織的團隊在取得金鑰後會先拉名冊,協作游標上顯示的就是組織給的名字,不必成員自己設定。
- **跨團隊總覽**:組織可列出自己的所有團隊(當代擁有者、成員數、憑證序號)與各團隊成員,是後續管理台的資料底。
- CLI 新增 `pnpm org name`(設定名冊顯示名)與 `pnpm org vaults`(列出本組織團隊)。

### 安全

- 名冊推送僅限組織管理連線;**拉取開放給任何已認證成員**——名字本就是團隊內的公開資訊,可信度來自組織簽章而非連線權限。
- 名冊條目帶單調序號,伺服器強制遞增:擋惡意伺服器把某人的顯示名回放成舊值(改名的用意常常正是要蓋掉舊身分)。
- 跨團隊總覽嚴格限於本組織已綁定的團隊:即使知道 vaultId,也列不到其他組織或未綁定的 vault。
- 顯示名長度設上限,擋灌爆側欄與游標標籤的版面。
- 邊界照舊:名冊全程不涉及任何內容金鑰,組織依然讀不到筆記。
- **誠實範圍**:協作游標的名字仍以自我宣告的形式在網路上傳輸(加密但未逐筆簽章),因此不加「已驗證」徽章;真正逐筆可驗的是留言與文件寫入的作者身分。

### 品質

- 端到端測試:成員拉得到可信顯示名與部門、偽造條目被濾掉、序號回退被拒、成員連線不得推名冊、組織列得到自己的團隊與成員但列不到他組織。全庫 514 項測試全綠。

## [0.19.1] - 2026-07-27

組織地基的走查修復:誤操作與失敗狀態現在會說出「該做什麼」,而不是丟一句籠統的錯誤。

### 修正

- **同步狀態缺文案**:團隊金鑰取得失敗時,狀態提示會顯示原始的字串代號而非人話。補上文案,並把組織憑證造成的失敗獨立成一種狀態——這類失敗不是網路壞掉,而是「團隊的組織歸屬對不上」,提示直接說明可能是伺服器未提供當代憑證或憑證比本機認可的更舊,請聯繫組織管理員重新簽發。
- **團隊面板吞掉失敗原因**:所有操作失敗一律顯示同一句話,看不出是綁定碼貼錯、憑證序號過舊,還是憑證指向的人不是成員。改為附上實際原因。
- **撤換擁有者時忘記背書前任**:這是最容易踩、後果最嚴重的一步(整個團隊——包含新擁有者自己——都解不開金鑰),原本只回報「簽章驗證失敗」。改為直接指出補救動作:請組織以更大的序號重新簽發並帶上前任擁有者公鑰。
- 綁定碼簽給其他團隊或其他組織時,補上最可能的成因;憑證序號未遞增時,一併回報伺服器目前的序號,管理員不必猜下一張要用多少。

### 品質

- 誤操作走查固化為回歸測試:五種踩法(綁錯團隊的碼、指向非成員、忘記背書前任、序號未遞增、未知組織連線)各自的訊息,以及「踩過之後救得回來」的完整復原路徑。全庫 497 項測試全綠。

## [0.19.0] - 2026-07-27

組織層地基:團隊可以隸屬於組織,信任錨從「某一任擁有者」上提到組織根——擁有者離職時,組織可直接指派新擁有者,成員不必重新加入,而且整個過程仍是密碼學可驗的。

### 新增

- **組織委任鏈**:組織根金鑰簽發管理員委任,管理員簽發團隊憑證(指名這個團隊當代的合法擁有者)。成員只需釘選組織根公鑰,即可驗出「現在誰是擁有者」,再以它驗證既有的金鑰信封、角色憑證與政策——原有的憑證鏈一行沒改,組織層是純加法。
- **擁有者可撤換**:組織透過獨立的管理連線指派新擁有者,**完全不需要離職擁有者配合**,也不必重建 vault 或重發邀請。新擁有者上任後在團隊面板執行「接管重簽」,以自己的簽章重發全員信封與憑證,前任的簽章即刻失去效力。
- **團隊面板的組織區塊**:顯示所屬組織與憑證序號、擁有者貼上綁定碼即可綁定組織、待接管時顯示提示與接管按鈕,並列出本人的身分公鑰供交給組織簽發憑證。邀請碼在團隊已綁組織時一併帶上組織根,新成員一開始就錨在組織而非某一任擁有者。
- **組織管理 CLI**(`apps/server` 的 `pnpm org`):產生組織根金鑰、簽發管理員委任、產生團隊綁定碼、直接連線指派擁有者。**在管理者自己的機器上執行**——根金鑰若與伺服器同處一地,「伺服器不可信」的前提就破了。

### 安全

- **治理平面與金鑰平面分離**:組織管理連線只能換發團隊憑證;讀寫文件、成員管理、金鑰信封一律拒絕。組織因此**拿不到任何內容金鑰、讀不到筆記**——能指派擁有者,不能解密。這是本次刻意的取捨(託管模式另行設計,尚未實作)。
- **反回滾**:團隊憑證帶單調序號,與金鑰紀元刻意解耦(綁紀元會造成「換擁有者→新擁有者輪換→憑證失效」的死結)。成員釘選看過的最大序號並拒收更舊的,擋惡意伺服器重放舊憑證把擁有者指回離職者;伺服器端也強制同一條單調性作為縱深。
- **fail-closed**:已綁組織的 vault 若收不到當代團隊憑證(例如惡意伺服器抑制),一律拒絕啟動同步,不默默退回舊的擁有者信任錨。
- **交接窗口收斂**:撤換當下,所有成員(含新擁有者本人)手上的信封仍是前任簽的。組織可在憑證中明示背書前任,**範圍嚴格限於金鑰信封**(前任本就知道該金鑰,不擴大任何權限);角色憑證、成員憑證與政策一律只認當代擁有者,過渡期前任簽的一律視同「未簽發」而非採信。前任因此無法在卸任後改動任何人的權限。
- 執行中的連線在撤換後熱換信任錨並重拉成員目錄,否則新擁有者重簽的目錄會被整份判為偽造、寫入全被丟棄。撤換時伺服器一律斷開該團隊**全體**成員連線,人人確定性重跑取得新錨——不留「最終會自癒但期間可能丟棄寫入」的窗口。
- 首次綁定組織必須由團隊擁有者自己發起,伺服器無法把團隊拉進沒人選擇的組織;已綁定者不接受改綁其他組織。

### 品質

- 端到端測試涵蓋:綁定後成員以組織為錨取得金鑰、離職擁有者零參與的撤換與接管、憑證抑制 fail-closed、舊憑證重放被拒、憑證指向非成員被拒、冒充管理員與未知組織被拒、組織連線取不到文件內容。桌面金牌測試走完整條路:撤換→接管→雙向協作照常→前任簽章失效→重放被擋。全庫 495 項測試全綠。

## [0.18.0] - 2026-07-24

對強制簽章模式的一輪對抗性審查與收緊:修掉一個讓惡意伺服器得以偷偷降級成員的反回滾缺陷。

### 安全

- **強制簽章反回滾改為 fail-closed**:先前成員每次重新取得團隊金鑰時,會用伺服器當下回傳的政策無條件覆蓋本機的強制態——惡意伺服器只要「抑制」(不回傳)政策,就能把已啟用強制簽章的成員偷偷降級回「容忍未簽章寫入」,重新打開注入窗口。現在成員明確區分三態:當代擁有者簽章政策的**開啟**、**關閉**、與**政策缺席**;唯有明確的當代政策才改變設定,缺席一律**保留本機既有狀態**,惡意伺服器抑制政策無法再造成降級。輪換時一律以新紀元重簽當前狀態(開啟或關閉),讓「關閉」也能正確跨紀元傳播,不會有成員永久卡在過期的強制態。
- 殘餘(信任源自首次接觸的本質限制,已載於 `SECURITY.md`):從未收過政策的成員——例如惡意伺服器自首次接觸就抑制——退回過渡容忍,與所有擁有者簽章狀態相同;以及持續在線的成員於下次重連或輪換才套用擁有者的切換(誠實伺服器的伺服器端拒收提供其間的即時保護)。

### 品質

- 政策 tri-state 端到端測試:政策未設/被抑制(直接刪除)回報「缺席」以保留 pin、開啟回報開啟、明確關閉回報關閉。全庫測試全綠。

## [0.17.0] - 2026-07-24

作者歸屬:留言作者從自我宣稱升級為密碼學可驗——標記「這則留言出自擁有者背書的合法成員」及其角色。

### 新增

- **可驗證的留言作者**:留言與回覆的作者改綁密碼學成員身分(`memberId`),而非先前的裝置代號。收件端對照擁有者背書的成員憑證目錄,在作者是當紀元合法成員時顯示「✓ 角色」徽章;裝置代號的舊留言、未知或已移除的作者則不標記。團隊成員變動(核准、移除、輪換)後徽章即時刷新。

### 安全

- 留言本身就是 doc 寫入,自 0.16 起已逐筆簽驗:唯讀成員或非成員的留言寫入在同步層即被拒,徽章只是把這層既有的真實性浮到介面。徽章表示「作者是擁有者背書的成員」;顯示名仍為成員自選標籤,合法編輯者在內容中冒填他人身分屬稽核範疇(見 `SECURITY.md` §4.1),非本功能所擋。
- 明確不做逐字 blame(CRDT 合併壓縮後 op 對作者的映射會碎裂);作者歸屬止於寫入層級。

### 品質

- 端到端斷言:成員同步後其已驗證目錄含擁有者(owner)與協作者(editor)及正確角色,涵蓋徽章的資料來源路徑。全庫測試全綠。

## [0.16.0] - 2026-07-23

寫入真實性收官:團隊 vault 的每筆編輯都由作者簽章、收件端驗證,並可由擁有者切換強制模式徹底拒收未簽章寫入——「誰寫了什麼」從社交信任升級為密碼學可驗。

### 新增

- **每筆寫入作者簽驗**:團隊 vault 的每筆增量與快照都帶作者的 Ed25519 簽章(簽密文雜湊,收件端先驗作者再解密)。收件端查成員憑證目錄取得作者可信公鑰、確認其為當紀元的擁有者/編輯者、驗簽通過才套用;驗不過的寫入(惡意中繼注入、被移除者殘留、唯讀成員寫入)沿用既有 poison-skip 丟棄並跨越,不卡序列。
- **強制簽章模式**:擁有者可簽發一份綁定金鑰紀元的 vault 政策開啟「拒收未簽章寫入」,關閉升級過渡期對舊用戶端的容忍窗口。政策隨金鑰信封原子下發、成員對擁有者公鑰驗證後套用並本機留存(抵抗惡意伺服器事後抑制);伺服器亦一併軟拒未簽章寫入作縱深防禦。團隊面板新增擁有者專屬開關。

### 安全

- 唯讀成員的「不得寫入」從可信伺服器邊界升級為**收件端可驗**:即使惡意伺服器願意中繼唯讀成員或被移除者的寫入,每個收件端都會因作者角色/紀元不符而拒收。
- 作者簽章綁定 `{寫入類型, docId, 金鑰紀元, 密文雜湊}`,不綁序號/裝置(轉發不帶、CRDT 套用冪等使重放無害);跨 doc 挪用、跨紀元重放、竄改內容一律驗不過。
- vault 政策綁金鑰紀元,輪換時以新紀元重簽,避免輪換後強制態靜默失效;偽造、跨 vault 挪用、竄改旗標一律驗不過。過渡窗口與殘餘威脅(伺服器僅能抑制可用性、合法編輯者的內容屬稽核範疇)已明列於 `SECURITY.md`。

### 品質

- 逐 update 簽章與 vault 政策的單元測試(正簽正驗、偽簽、跨 vault、跨紀元、竄改、截斷全拒),以及兩支端到端金牌:合法協作在簽驗/強制下照常收斂;偽造作者與未簽章注入(即使以團隊金鑰加密解得開)一律被拒不套用。全庫測試全綠。
- 桌面整合測試改為序列執行,消除多支端到端測試並行時互相爭搶 CPU 造成的非邏輯性 flaky。

## [0.15.0] - 2026-07-23

成員憑證:團隊成員的簽章公鑰由擁有者背書、可驗,為「寫入作者驗證」鋪好地基。

### 新增

- **成員憑證與可信目錄**:擁有者在核准成員、改角色或輪換金鑰時,簽發一份綁定「這位成員的簽章公鑰 ↔ 成員 id」的憑證。任一成員都能拉取全團隊的成員憑證目錄,逐筆對擁有者公鑰驗證——從此能密碼學地確認「這個成員 id 對應的簽章公鑰確實是擁有者認可的」。這是後續「每筆編輯的作者可驗」的前置基建;目錄本身也讓成員能驗證彼此身分。

### 安全

- 成員 id 由簽章公鑰導出(`sha256(pubSign)`),憑證不單獨簽 id、由驗證者自算;團隊 id 由驗證者自帶。惡意中繼捏造假成員、竄改某成員的公鑰、或跨團隊挪用憑證,一律驗不過而被目錄濾除。憑證綁定金鑰紀元,輪換即作廢整代。
- 目錄拉取開放給任一認證成員(簽章公鑰本是公開資訊,真實性由每筆憑證的擁有者簽章保證);簽發仍限擁有者。

### 品質

- 成員憑證單元測試(正簽正驗且成員 id 由公鑰導出、非擁有者簽、跨團隊、公鑰/角色竄改、截斷全拒)與端到端測試(一般成員拉目錄取得全員可信公鑰、惡意摻假被濾、移除成員後從目錄消失)。全庫測試維持全綠。

## [0.14.0] - 2026-07-23

真機走查後的一輪修復:受限空間更誠實、檢視者真唯讀、團隊流程更順,並修掉數個同步收斂與安全問題。

### 新增

- **空間內直接新建筆記**:側欄每個空間標題列新增「＋」,建立的筆記自動歸入該空間,省去「先建再移」。
- **被移出團隊的明確提示**:被擁有者移除後,側欄顯示紅色橫幅、同步指示轉紅並停止重連,不再靜默無感。

### 變更

- **待核准免重開**:以邀請碼加入後,擁有者一核准即自動開始同步,不必手動重開 vault。
- **加入團隊帶顯示名**:建立或加入團隊時預設帶入系統使用者名,協作游標不再一律顯示「訪客」。
- **名單外成員看不到受限空間**:受限空間及其筆記在無權成員的側欄完全隱藏;先前已同步的本地檔案在失去授權時移入回收桶(受限前已在對方裝置上的明文屬已知限制,無法回收)。

### 修復

- **檢視者真唯讀**:檢視者角色的編輯器與留言收斂為唯讀,不再出現「能編輯但同步不上去」的假象;伺服器對唯讀成員的寫入改為軟拒,避免離線編輯重連時陷入斷線迴圈。
- **空間存取變更即時反映**:授權或撤銷後,名單內成員免重開自動補齊內容、名單外成員的側欄即時更新。
- 團隊面板與空間相關對話框的多處排版與按鈕樣式修正。

### 安全

- **受限空間金鑰不外洩**:受限與否改以金鑰信封層為權威判定(與金鑰原子取得),消除「名單已同步、金鑰未到」的競態窗口下以團隊金鑰誤加密受限內容的風險;加密端加上「無該空間金鑰即拒絕加密」的硬性防線。
- **輪換重加密不被靜默略過**:金鑰輪換撞上既有快照點時,以新金鑰的密文為準覆蓋,避免舊金鑰快照殘留。
- **失去授權的本地移除不誤傳共享刪除**:無權而被移入回收桶的筆記只卸下同步,絕不從共享中繼資料刪除路徑,免得連帶刪掉名單內成員(含擁有者)的筆記。

### 品質

- **同步收斂修復**:快照更新現在會廣播給同 vault 其他連線,落後成員不再因增量被截斷而卡在舊進度;跳號補洞加上節流,避免自我再生的緊迴圈;解不開的增量以本地快照跨越,不再永久卡住序列。全庫測試維持全綠。

## [0.13.0] - 2026-07-23

空間存取:某個空間只給部分成員——受限空間有自己的金鑰,名單外的人拿到密文也解不開。

### 新增

- **空間成員子集**:團隊 vault 的擁有者可把任一自訂空間設為「受限」,勾選哪些成員能存取。受限空間使用**獨立的隨機金鑰**(不由團隊金鑰衍生),只包給名單內成員與擁有者——名單外成員即使持有團隊金鑰,對該空間的密文也無能為力。這是密碼學邊界,不是介面隱藏。
- **名單外成員的乾淨體驗**:受限空間的筆記在名單外成員的裝置上完全不落地——新筆記不出現、不下載、無解密錯誤;受限前已同步的檔案停在最後可讀狀態。之後獲授權,內容自動補齊。
- **團隊面板「空間存取」**:每個空間顯示「全團隊」或「限 N 人+擁有者」,勾選成員後一鍵「儲存並輪換金鑰」;隨時可恢復開放全團隊。

### 變更

- **存取變更即輪換**:授權、撤銷、恢復開放都伴隨一次金鑰輪換——每次輪換為每個受限空間換發新金鑰、只重包給當下名單,被撤銷者的舊空間金鑰立即對新內容失效。沿用 0.11.0 輪換管線的全部保證(寫入柵欄、崩潰續跑、成員自癒)。
- **留言跟著空間走**:筆記的留言現在與筆記本體用同一把空間金鑰加密——受限筆記的留言同樣只有名單內成員讀得到(先前留言一律以團隊金鑰加密)。

### 安全

- **前向限制(明確界定)**:把「已有內容」的空間轉為受限,只保護此後的內容——受限前的內容,名單外成員的裝置上早已持有明文副本與舊密文。要放機密內容,先建受限空間、再把內容放進去。

### 品質

- 金牌測試(三成員經真伺服器):名單內成員以空間金鑰收斂編輯;名單外成員新筆記不落地、既有檔案停更、以團隊金鑰解伺服器密文被拒;撤銷後舊空間金鑰對新內容失效、擁有者恆可解。另有空間金鑰衍生、名單模型、伴生 doc 歸屬的單元測試。全庫 436 測全綠。

## [0.12.0] - 2026-07-22

角色憑證:成員的角色由擁有者簽章、客戶端可驗,伺服器無法捏造。

### 新增

- **客戶端可驗的角色憑證**:擁有者核准成員、改角色或輪換金鑰時,都會對「這個 vault、這位成員、這個角色、這個金鑰紀元」簽出一份 Ed25519 憑證,經伺服器分發;成員取金鑰時以邀請時取得的擁有者公鑰(信任錨)驗證。**角色指派從此防竄改**——被攻陷的伺服器無法把檢視者謊稱成編輯者,補上 0.10.0 明定的信任邊界缺口。

### 安全

- **偽造即拒**:憑證簽章驗不過(偽簽、跨 vault 或跨成員挪用、內容竄改)時,成員端直接中止金鑰載入,不會拿著假角色運作。
- **舊憑證重放防護**:憑證綁定金鑰紀元,輪換金鑰即作廢整代舊憑證——伺服器重放輪換前的憑證不會被採信。同一紀元內改角色後的舊憑證重放仍屬已知限制(兩者皆為真簽),擁有者輪換金鑰即可沖洗;每筆編輯的作者驗證留待後續版本。
- **平滑升級**:升級前已核准的成員尚無憑證,維持原行為(顯示本地既知角色);擁有者下次改角色或輪換金鑰時自動補發。

### 品質

- 憑證單元測試(正簽正驗、偽簽、挪用、內容竄改、截斷)與端到端測試(核准/改角色簽發流、資料庫植入偽簽憑證即拒、輪換重簽與舊紀元重放降級、非擁有者簽發被拒、移除成員清除憑證)。全庫 429 測全綠。

## [0.11.0] - 2026-07-22

金鑰輪換:移除成員後整個 vault 換新金鑰並重新加密,離線舊金鑰從此解不開新內容。

### 新增

- **移除成員即輪換金鑰**:擁有者移除成員後,自動產生新的團隊金鑰、重新包給所有留任成員,並把 vault 內容全部以新金鑰重新加密。被移除者即使留有離線舊金鑰與舊快取,也解不開此後的任何內容——補上 0.10.0 已知限制中的密碼層前向保密。
- **手動輪換**:團隊面板新增「輪換金鑰」按鈕,自動輪換失敗(例如網路中斷)時可重試,也可作例行輪換。
- **待核准標記**:成員清單標示尚未核准的成員;輪換只把新金鑰重新包給已核准成員,不會繞過指紋核對。

### 變更

- **輪換後分享連結作廢**:分享連結內含由團隊金鑰衍生的筆記金鑰,輪換後一律失效並即時切斷已連線的分享檢視;需要時以新金鑰重新建立分享。個人 vault 的分享不受影響。

### 安全

- **紀元寫入柵欄**:每個團隊 vault 帶單調遞增的金鑰紀元(epoch),輪換 commit 後伺服器立即拒絕舊紀元的寫入——確保輪換窗口內不會有舊金鑰密文混入共享日誌造成永久污染。
- **崩潰安全**:輪換全程冪等——commit 前任何失敗都不留半套狀態(紀元不動、舊金鑰續用);commit 後若重加密中斷,擁有者重啟會自動續跑補完(每篇重加密都等伺服器確認落盤才算數)。
- **成員自癒**:線上成員收到輪換廣播即暫停推送、取回新金鑰後無縫收斂;離線錯過輪換的成員重連時發現紀元落後,同樣自動取回新金鑰並重新拉齊,期間絕不套用解不開的資料。

### 品質

- 輪換全鏈測試:伺服器層(紀元 CAS 只接受恰為當前+1、柵欄拒舊紀元寫入、輪換廣播、分享作廢與踢線)與桌面金牌(三成員經真伺服器——留任者收斂新金鑰並雙向編輯、被移除者舊金鑰解不開重加密內容、重加密冪等、離線者重連自癒、未拉齊即中止且紀元不動)。全庫 419 測全綠。

## [0.10.0] - 2026-07-21

團隊角色與授權:owner / editor / viewer,伺服器強制讀寫權,踢人立即生效。

### 新增

- **成員角色(owner / editor / viewer)**:團隊 vault 現在區分角色。**擁有者**全權並可管理成員;**編輯者**可讀寫;**檢視者**唯讀。擁有者產生邀請碼時就決定被邀者的角色,加入即生效。桌面團隊面板可顯示每位成員的角色、隨時把成員在編輯者/檢視者間切換,或移除。
- **伺服器逐訊息授權**:團隊 vault 上,只有擁有者/編輯者能寫入(含改動筆記路徑、空間等共享結構);檢視者的任何寫入一律被伺服器拒絕,但仍可讀取與即時看到協作。個人 vault 完全不受影響。
- **移除/降級立即踢線**:擁有者移除成員或把編輯者降為檢視者時,對方的**活躍連線當場切斷**——不再等到下次斷線。被移除者無法重連(邀請碼一次性、已消耗)。

### 變更

- 升級既有 0.9.0 團隊 vault 時,伺服器會自動為成員名冊補上角色欄位(既有成員預設為檢視者,擁有者維持擁有者身分);**擁有者可在團隊面板重新指派各成員的角色**。

### 安全

- **授權的信任邊界(明確界定)**:在全盲中繼的端對端加密模型下,角色的**讀寫強制是「可信伺服器」層級的控制**(防濫用、防誤操作),而非密碼學保證——檢視者本身持有團隊金鑰,一個被攻陷的伺服器理論上仍可接受其寫入。**機密性與金鑰分發**維持不信任伺服器的端對端加密(空間金鑰信封的擁有者簽章才是客戶端可驗的密碼學邊界)。把角色指派也變成客戶端可驗、以及每筆編輯的作者驗證,留待後續版本。

### 品質

- 角色全鏈測試:store 角色 CRUD 與邀請碼帶角色、協議往返;伺服器端對端(編輯者可寫 / 檢視者寫入含 vault-meta 皆拒 / 任何成員可讀、移除與降級即時踢線並擋重連、非擁有者管理指令被拒、升級自癒讓遷移後的擁有者仍能寫入)。全庫 404 測全綠。

### 已知限制(留待後續)

- **踢人尚未輪換金鑰**:移除成員只切連線與刪其金鑰信封,不重換團隊金鑰;被移除者若留有離線舊快取,仍能解先前已同步的舊內容(對「新內容」則因無法重連而取不到)。密碼層的前向保密(換金鑰 + 重加密)為下一階段。

## [0.9.0] - 2026-07-21

團隊協作正式落地:多人各以自己的身分,共享同一個加密 vault。

### 新增

- **團隊 vault(邀請 + 空間金鑰盲中繼)**:一個團隊 vault 有一把隨機的**團隊金鑰**,用每位成員的公鑰包成信封,經**全盲伺服器**轉交;成員以自己的身分金鑰解出後即可協作。伺服器看不到金鑰明文,也讀不到內容。加入流程:擁有者產生**一次性邀請碼**交給對方(out-of-band),對方憑碼加入後,擁有者**核對其公鑰指紋**再核准(把團隊金鑰包給他)。核准前成員處於「等待授權」狀態,不會誤讀任何東西。擁有者可**移除成員**。桌面新增團隊面板:建立 / 加入團隊 vault、產生邀請碼、成員清單與指紋核對、核准 / 移除。
- **身分種子上 OS keychain**:成員身分的種子改由系統金鑰鏈(macOS Keychain / Windows DPAPI)加密後才落盤;keychain 不可用時優雅退回、並相容既有明文檔。因為這把種子的洩漏面在團隊時代跨越多個 vault,值得比 vault 密語更強的保護。

### 變更

- **團隊 vault 強制身分認證**:設有擁有者的 vault 一律走 challenge-response 身分認證,拒絕只帶 token 的舊式連線,並擋下非成員的寫入——順帶堵掉「任何持 token 者上傳一份垃圾快照即可截斷他人真實編輯」的可用性攻擊。個人 vault 的密語同步完全照舊,零改變。

### 安全

- **信封由擁有者簽章 + 情境綁定**:空間金鑰信封帶擁有者的 Ed25519 簽章,成員以 out-of-band 已知的擁有者指紋驗簽——擋下「惡意伺服器自造信封 + 自造中繼資料,餵新成員一個偽造的 vault」。信封的加密情境綁定 vault / 金鑰 / 版本 / 收件人,防跨情境挪用重放;解封拒絕退化的金鑰交換結果。

### 品質

- 團隊全鏈測試:金鑰信封 wrap/unwrap(含偽造、竄改、跨情境、退化點防線）、協議往返、伺服器授權(邀請碼單次 / 綁 vault / 過期、擁有者專屬操作、信封只回本人、team vault 拒舊式連線)、bootstrap 死結防護與 pending→ready、擁有者管理連線、邀請 bundle 編解碼、身分種子 keychain 往返;**金牌**:兩個不同身分的成員經真伺服器雙向收斂、以團隊金鑰加解密互通。全庫 393 測全綠。

### 已知限制(留待後續)

- **踢人後金鑰未輪換**:移除成員只刪其名冊列與信封,不重換團隊金鑰;被移除者若留有舊金鑰快取,仍能離線解舊內容。真正的踢人(輪換 + 重包給留任者）與 owner/editor/viewer 角色強制留待下一階段。
- **per-space 成員子集**:本版團隊是「整個 vault 共享」,尚無「某空間只給部分人」。資料模型已為此預留。

## [0.8.0] - 2026-07-21

本地優先補完 + 團隊協作的身分地基:空間與留言不再需要同步,並為團隊功能鋪好成員身分。

### 新增

- **成員身分(團隊協作的地基)**:每台裝置持有一把長期的**非對稱成員身分**——單一隨機種子衍生出簽章(Ed25519)與金鑰包裝(X25519)兩把子金鑰。連線時對伺服器做 **challenge-response**(簽名綁定伺服器亂數 + vault + 成員,防重放與跨 vault 挪用),證明「你是誰」而不只是「你知道 token」。伺服器把成員公鑰記進 per-vault 名冊(公鑰**首見即釘選**,擋冒名)。身分獨立於任何 vault 密語,存本機獨立檔、可匯出匯入到其他裝置。**這是團隊空間的樞紐**:未來邀請成員 = 用對方的公鑰把空間金鑰包起來經全盲伺服器轉交,難題從「不可能」降到「可做」。個人使用者無感——現有 vault 的密語同步完全照舊。

### 變更

- **空間與留言不再需要啟用同步**:先前「空間分組」與「留言討論」都掛在同步層管理的中繼資料上,未開同步時整組退化(側欄變平面清單、留言面板停用)。現在把中繼資料的所有權從同步層抽離,交由 vault 本身持有——**純本地、沒開同步的 vault 一樣能建空間、移動/複製筆記、留言討論**,同步啟用時才額外把它們推上雲。這修正了本地優先產品不該有的破口。

### 品質

- 身分全鏈測試:金鑰生成/簽章/序列化 6 例、協議往返、伺服器 challenge-response 端對端(含搶註、重放、跨裝置匯入)、桌面身分檔持久化;純本地空間 5 例 + 純本地留言 2 例守住「無同步也可用」不變量。
- 安全審查(密碼學 + 認證):修掉一個搶註漏洞(伺服器綁定成員 id 為其公鑰雜湊,擋任意 id 配自帶金鑰霸佔他人身分),並硬化身分檔落盤(原子寫、壞檔不靜默換身分)。桌面測試增至 97,全庫 347 測全綠。

### 已知限制

- 團隊空間本身(邀請、角色、用公鑰包裝空間金鑰)尚未開放——本版只鋪身分地基,伺服器記錄成員但**尚不以成員身分做授權**,存取仍靠既有 token + 密語。成員名冊在本版是諮詢性紀錄。

## [0.7.0] - 2026-07-20

空間與權限地基:把筆記分組到帶金鑰的「空間」,為團隊協作鋪路。

### 新增

- **空間(vault → 空間 → 筆記三層地基)**:側欄可把筆記分組到「空間」。每個空間是一級「帶金鑰單元」——空間金鑰往下衍生每篇筆記的金鑰;**預設空間沿用主金鑰,既有 vault 零遷移、金鑰一位元組不變**。可建立、改名空間(預設空間也可改名),分組可折疊。這是團隊/組織協作的架構地基:空間金鑰的取得已抽象,未來團隊空間只換「金鑰來源」(改為隨機生成 + 成員公鑰包裝),加解密與同步下游不動。
- **跨空間移動與複製**:移動筆記到別的空間會跨金鑰邊界——確認後以新空間金鑰重新加密,並重推快照截斷舊金鑰增量,讓其他裝置以新金鑰解出(跨裝置收斂)。複製則在目標空間生一篇**全新筆記**(新 docId、自出生即以目標空間金鑰加密),原筆記原封保留。
- **空間變更稽核紀錄**:建立、改名、移動、複製都留 append-only 事件,側欄可查(單人「好追查」,團隊層將成合規稽核命脈)。
- **Homebrew 一行安裝**:`brew install --cask cluion/tap/stele`;發版 CI 自動更新 tap 的 Cask 版本與雜湊。未簽章,首次開啟右鍵 Stele.app → 開啟。

### 品質

- 空間全鏈測試:金鑰衍生 7 例(含「預設空間與舊金鑰位元組相同」零遷移守門、新空間兩層互不可解)、空間模型 9 例(登記/歸屬/稽核/CRDT 合併)、真伺服器端對端 4 例(零遷移同步、跨裝置移動收斂、後加入裝置 bootstrap、複製雙篇收斂)。以真 Electron 端對端金牌驗證「建立空間 → 側欄分組 → 移動筆記 → 稽核記錄」。

### 已知限制

- 空間目前需啟用加密同步(空間資料掛在 vault-meta,由同步層管理);未啟用同步的 vault,側欄退回平面清單。與留言同一約束。

## [0.6.0] - 2026-07-18

留言與討論:錨定到文字範圍的協作討論串,獨立資料型別、不污染筆記。

### 新增

- **留言與討論**:在編輯器選取文字即可留言,他人可回覆成討論串、可標記已解決或刪除。留言錨定到文字範圍(Yjs relative position),你我插字時錨點自動位移,原文被刪則標「原文已刪」。源碼模式字元級高亮、所見即所得模式整段高亮;留言面板列出全篇討論、點引用即捲到原文。
- **留言是獨立的伴生資料**:每篇筆記的留言存在與筆記本體不同的 CRDT doc,**不進 .md 鏡像、不污染筆記歷史**;走同一條端對端加密同步,留言 doc 自得一把獨立子金鑰,伺服器仍全盲。留言 doc 的 id 由筆記 id 決定性衍生,兩裝置各自算出同一個,免對照表也不會併發分裂。

### 品質

- 留言全鏈測試:討論串模型與範圍錨定 6 例(含 CRDT 合併、錨點位移/塌縮)、伴生 doc 真伺服器端對端 2 例(跨裝置留言/回覆同步、對方解得出錨定、留言不進 .md);桌面 smoke 增至 19。以真 Electron 端對端驗證「選取文字→留言→同步到第二裝置→對方解出錨定」完整往返。

### 已知限制

- 留言需啟用加密同步(伴生 doc 由同步層管理);未啟用同步的 vault,留言面板會提示而非靜默失效。

## [0.5.0] - 2026-07-18

可編輯分享閉環:貼上分享連結,在桌面開臨時協作視窗即時共編。

### 新增

- **消費可編輯分享連結**:桌面貼上別人給的分享連結(側欄 ⇲ 鈕、歡迎畫面入口或 Cmd/Ctrl+Shift+O),即以「臨時協作視窗」開啟。可寫連結能即時編輯,內容即時同步回來源方 vault;唯讀連結以唯讀開啟。**消費者不需要自己的 vault**,關掉視窗即結束,本地不留檔。分享功能自此雙軌俱全:唯讀走瀏覽器檢視器,可編輯交桌面協作。
- **端對端加密不打折**:金鑰只從連結 fragment 取得且留在桌面主行程(不進有 IPC 面的 renderer 主世界);伺服器仍全盲只中繼密文;唯讀權限由伺服器回報並在編輯器層鎖定輸入。

### 品質

- 新增消費端測試:連結解析 4 例、SharedSession 真伺服器端到端 3 例(可寫回寫來源磁碟、唯讀不外洩、撤銷即時失效);桌面 smoke 增至 18 項。以真 Electron 端到端驗證「貼連結→協作視窗打字→回寫來源磁碟」完整往返。
- 修穩 CI:補 `packageManager` 讓 GitHub Actions 的 pnpm setup 認得版本、smoke 於 CI 固定 zh-TW locale,發版與 CI 兩條 workflow 皆綠。

## [0.4.0] - 2026-07-18

桌面安裝檔與自動發版:一鍵下載的 dmg / AppImage / deb,推 tag 即自動打包並發佈。

### 新增

- **桌面安裝檔打包**:`apps/desktop` 接上 electron-builder,產出 macOS dmg(x64 + arm64 雙架構)、Linux AppImage 與 deb,附石碑應用圖示。產物走 esbuild 全 bundle,asar 只收 `dist/` 不含 node_modules,dmg 檔名帶架構後綴避免雙架構互相覆蓋。
- **CI 自動發版**(新 `.github/workflows/release.yml`):push `v*` tag 觸發,`build` job 在 macOS 與 Linux runner 並行打包三平台安裝檔,`release` job 收齊產物後從 `CHANGELOG.md` 抽出對應版本節當 release notes,`gh release create` 一次帶齊安裝檔直接發佈;發佈前斷言 tag 版本與 `package.json` 一致,擋下忘記升版號的誤發。`workflow_dispatch` 另備只建置不發佈的 dry-run。

### 品質

- 發版流程全自動化,原本手動的跨平台打包與建 Release 交由 CI 完成;Linux 產物改由 Linux runner 實際打包,不再是 mac 上跨建。發版流程文件(`plan/RELEASE.md`)同步更新。

## [0.3.0] - 2026-07-17

分享連結:把單一筆記唯讀分享到瀏覽器,伺服器仍全盲。

### 新增

- **唯讀分享連結**:右鍵任一筆記即可建立分享連結,對方用瀏覽器就能開,免安裝、免帳號。桌面對話框含複製連結與撤銷清單。
- **網頁檢視器**(新 `apps/viewer`):與桌面同一套 editor-core schema 唯讀渲染,連結、表格、callout 呈現一致。伺服器同埠掛靜態頁,`shareId` 只在前端解析。
- **端對端加密延伸到分享**:每則分享匯出「單一 doc 金鑰」(HKDF 單向,vault 主金鑰不外洩),金鑰只走 URL fragment(`#` 之後)不進伺服器;伺服器新增 shares 表管理作用域與撤銷,唯讀連線鎖定單一 doc 且拒所有寫入型訊息。

### 修正

- **撤銷即時生效**:撤銷分享會當場切斷既有連線,撤銷後的內容一個字都拿不到(原本只擋新連線,已連上的可續讀);且撤銷嚴格綁 vault,猜中他人 `shareId` 也踢不掉對方連線。
- **連結 href 淨化**:編輯器與檢視器只放行 `http`/`https`/`mailto` 與相對連結,`javascript:`/`data:` 等一律剝除(貼上 HTML 會繞過 markdown-it 的內建過濾,渲染器主世界有 IPC 面,不能只靠上游解析器設定)。
- **檢視器安全標頭**:分享頁補上 CSP(`default-src 'none'`、腳本只信同源、`frame-ancestors 'none'`)、`nosniff`、`no-referrer`;`frame-ancestors` 下在 HTTP header,`<meta>` 版另備為自架保底。

### 品質

- 分享全鏈測試:金鑰匯出/單 doc 解密、協議往返、伺服器作用域與權限、撤銷即時與跨 vault 隔離、桌面「建立→列出→撤銷」真實往返(驗證 fragment 金鑰可還原且不落伺服器)、檢視器連結解析。
- 桌面 smoke 增至 17 項,新增分享對話框開啟/建立/關閉全鏈。

## [0.2.0] - 2026-07-17

即時多人協作:加密的在場指示與遠端游標。

### 新增

- **即時協作 awareness**:多人開同一 vault 時,即時顯示誰在看哪篇筆記(彩色頭像,顏色由裝置穩定衍生)。
- **遠端游標**:兩種編輯器模式都渲染協作者游標——源碼模式為字元級精確 caret 與選取範圍(Y.Text relative position,並發編輯下不漂移);所見即所得模式為塊級段落標記與名字標籤。
- **端對端加密**:awareness 與游標資料以同一把 doc 金鑰加密,伺服器為 blind relay,看不到誰在哪、也不落盤(ephemeral)。

### 修正

- 本地游標回報加上節流(~90ms),打字期間不再每按鍵一次加密廣播。
- 協作熱路徑改用編輯器增量維護的區塊映射,不再每次按鍵/在場更新整份重新解析 Markdown。
- 同步引擎:awareness 對非法 docId 拒絕(防資源耗盡)、切 vault 重置在場、stop 後不復活實例。

## [0.1.0] - 2026-07-17

首個公開版本:本地優先單機 MVP + 端對端加密同步 + 自架伺服器。

### 新增

- **編輯器核心**:ProseMirror 真 WYSIWYG + CodeMirror 6 源碼模式,塊級重序列化保位元組穩定;不支援語法走 opaque 原文節點。
- **Wikilink 生態**:`[[ ]]` 自動完成、點擊導航、就地建檔、改名連動全庫改寫。
- **雙鏈**:反向連結面板、自寫力導向關聯圖。
- **每日筆記**:`{{date}}` 模板、`.stele/config.json` 可自訂。
- **搜尋與切換**:CJK bigram 全文搜尋、快速切換器。
- **設計系統**:日石英 / 夜燭石 雙主題(原生設計,非反轉)。
- **CRDT 狀態持久化**:`.stele/` 保存二進位狀態,開檔吸收外部差異,歷史跨啟動延續。
- **端對端加密同步**:passphrase → scrypt → 主金鑰,per-doc HKDF 子金鑰 + AES-256-GCM;伺服器只見密文。
- **自架同步伺服器**:Node + ws + better-sqlite3 的 blind relay,單 Docker 一行起服;雙路徑同步(快照 + 增量)、離線佇列、路徑 LWW。
- **i18n**:zh-TW / en,CI 缺譯檢查。

### 品質

- 209 個單元/整合測試,含 8 輪固定 seed 多裝置混沌測試(隨機編輯/刪除/斷線/中殺,零資料遺失為底線)。
- 16 項 smoke 測試涵蓋桌面全鏈。
- CI:lint、typecheck、test、授權政策檢查、smoke。

[0.10.0]: https://github.com/cluion/stele/releases/tag/v0.10.0
[0.9.0]: https://github.com/cluion/stele/releases/tag/v0.9.0
[0.8.0]: https://github.com/cluion/stele/releases/tag/v0.8.0
[0.7.0]: https://github.com/cluion/stele/releases/tag/v0.7.0
[0.6.0]: https://github.com/cluion/stele/releases/tag/v0.6.0
[0.5.0]: https://github.com/cluion/stele/releases/tag/v0.5.0
[0.4.0]: https://github.com/cluion/stele/releases/tag/v0.4.0
[0.3.0]: https://github.com/cluion/stele/releases/tag/v0.3.0
[0.2.0]: https://github.com/cluion/stele/releases/tag/v0.2.0
[0.1.0]: https://github.com/cluion/stele/releases/tag/v0.1.0
