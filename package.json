{
  "name": "stele",
  "private": true,
  "version": "0.0.0",
  "description": "Local-first knowledge base. Carved to last.",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "pnpm --filter @stele/editor-core exec tsc --noEmit && pnpm --filter @stele/desktop exec tsc --noEmit",
    "test": "pnpm -r test",
    "licenses:check": "node scripts/check-licenses.mjs",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm licenses:check"
  },
  "devDependencies": {
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.64.0"
  }
}
